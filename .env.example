# Supabase URL + anon key are hardcoded in `src/integrations/supabase/client.ts`.
#
# Verbose `[EW]` console diagnostics in production builds (optional):
# VITE_DEBUG=true
#
# Server / Edge Functions only — never prefix with `VITE_`
# SUPABASE_SERVICE_ROLE_KEY=
# RESEND_API_KEY=
APP_URL=http://localhost:5173
